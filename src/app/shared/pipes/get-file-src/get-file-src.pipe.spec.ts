import { GetFileSrcPipe } from './get-file-src.pipe';

describe('GetFileSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFileSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
